#include <stdio.h>
struct point{
	int x,y;
}p[101];
int k,n;
bool ans[101]
int main(int argc,char *argv[]){
	k=1;
	n=1;
	while(n!=0){
		scanf("%d",&n);
		if(n>=n){
			scanf("%d%d",p[i].x,p[i].y);
			for(int i=2;i<=n;i++){
				scanf("%d%d",p[i].x,p[i].y);	
			}
		}
	}
	return 0;
}
